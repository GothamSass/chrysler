
for event, slug in public.events._data
  if event.layout !== false
    eventName = event.name.replace(/\s+/g, '-').toLowerCase()
    .gotham-sass--event
      h1.event-name: a(href="/events/#{eventName}")= event.name
      p #{event.description}
        time.dt-start(datetime="2013-06-30 12:00")
          sup #{event.start_date}
        time.dt-end(datetime="2013-06-30 18:00")
          sup #{event.end_date}
        span.p-location #{event.venue.name}
